'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function DashboardRedirect() {
  const router = useRouter();

  useEffect(() => {
    // Redirect to the integrated security dashboard
    router.replace('/security');
  }, [router]);

  return null;
}
