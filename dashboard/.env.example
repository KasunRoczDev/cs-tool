# Backend the Next.js server proxies to (server-side rewrite target).
BACKEND_URL=http://localhost:4000

# Optional: absolute API base for the browser. Leave blank to use same-origin (rewrites).
NEXT_PUBLIC_API_BASE=
